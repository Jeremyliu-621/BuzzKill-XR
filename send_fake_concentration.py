# import socket
# import time
# import math

# print("send fake working")
# sock = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)
# t = 0.0

# while True:
#     v = 0.5 + 0.5 * math.sin(t
#     sock.sendto(f"{v}".encode(), ("127.0.0.1", 5005))
#     t += 0.05
#     time.sleep(0.02)
